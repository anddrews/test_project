import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    width: 200,
    height: 300,
  },
  img: {
    width: 200,
    height: 300,
    position: 'absolute',
    top: 0,
    left: 0,
  },
});
